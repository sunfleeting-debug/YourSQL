# 《大型平台软件设计实习》项目计划书：MiniSQL 小型数据库系统设计与实现

## 项目概述

### 课程背景

本计划书依据中南大学计算机学院《大型平台软件设计实习》指导书（编撰人：姚鑫、桂宁、邓磊）与课程 PPT（姚鑫、邓磊、桂宁）编写，用于指导本次实习项目"小型数据库系统（MiniSQL）"从需求理解、设计、编码到验收的全过程。实习以**分步构建一个可运行的简化数据库系统**为总体目标，贯通编译原理、操作系统与数据库三门课程的核心知识。

### 总体目标：三步走

1. **SQL 编译器**（编译原理）：实现词法分析 → 语法分析 → 语义分析 → 执行计划生成，把 SQL 文本变成可执行的逻辑执行计划。
2. **页式存储系统**（操作系统）：实现以页为单位的存储模型（页分配、释放、读写）与缓存机制（LRU / FIFO），提供底层数据访问接口。
3. **小型数据库系统**（数据库）：在前两步基础上实现执行引擎、存储引擎与系统目录（Catalog），能完整执行 CREATE TABLE / INSERT / SELECT / DELETE。

### 核心主线

整条数据链路为：**SQL → Token → AST → 语义检查 → 执行计划 → 优化 → 执行引擎 → 数据页 / 磁盘**。编译器不是孤立实验，而是数据库的"前端"：它输出的执行计划将直接驱动后续执行引擎与存储系统，最终体验"从语言到系统"的完整链路。

## 具体要求梳理

以下要求全部来自指导书与课程 PPT，按三大模块整理，是验收的硬性依据。

### 模块一：SQL 编译器（4 个阶段）

| 阶段 | 输入 | 关键要求 | 输出 / 错误要求 |
|---|---|---|---|
| 词法分析 Lexer | SQL 文件或标准输入，支持多条语句，保留行号列号 | 识别关键字（SELECT/FROM/WHERE/CREATE/TABLE/INSERT/INTO/VALUES/DELETE）、标识符、常量（数字、字符串）、运算符（= != > >= < <= + - * /）、分隔符（( ) , ;）；跳过空白与注释（-- 单行、/* */ 多行）；处理字符串转义（'Tom''s book'）；关键字大小写不敏感 | Token 流，每项为 [种别码, 词素值, 行号, 列号]；非法输入（@、未闭合字符串、非法数字）返回错误类型 + 位置 + 原因，不得崩溃 |
| 语法分析 Parser | Token 流 | 基于显式提交的 SQL 子集文法（grammar.md）构建 AST，代码实现与文法一致；至少支持 CREATE TABLE、INSERT、SELECT、DELETE 四类语句；表达式优先级为 NOT > 比较 > AND > OR，支持括号；可采用递归下降、LL(1) 表驱动或 LR 方法 | 结构化 AST（语句节点 + 表达式节点，不是 Token 简单堆叠）；语法错误输出位置（行 + 列）+ 实际符号（unexpected token）+ 期望符号集合 |
| 语义分析 Semantic | AST + Catalog | 表存在性、列存在性检查；名字绑定（标识符指向 Catalog 中的具体列）；类型一致性检查（INT + INT → INT，INT > INT → BOOL，VARCHAR = VARCHAR → BOOL，BOOL AND BOOL → BOOL，NOT BOOL → BOOL，INT + VARCHAR → ERROR）；INSERT 列数、列序、值类型匹配检查；构建并维护模式目录 Catalog（createTable / findTable / findColumn / getType） | 语义检查结论；错误格式为 [错误类型, 位置, 原因说明]，例如 SemanticError at line 3, column 11: operator '+' cannot be applied to INT and VARCHAR |
| 执行计划生成 Planner | 语义检查通过的 AST | 将 AST 转换为逻辑执行计划，算子包括 CreateTable、Insert、SeqScan、Filter、Project；转换规则明确：FROM 决定数据源 → WHERE 生成 Filter → SELECT 列表生成 Project；Plan 节点只保存执行所需信息 | 树形结构 / JSON / S 表达式任选其一，结构必须明确；遇到不支持语法或缺失语义信息时输出 Error 提示 |

### 模块二：页式存储系统（操作系统知识实践）

| 子模块 | 要求 | 接口 / 输出 |
|---|---|---|
| 页式存储模型 | 每页大小固定（如 4KB），页编号唯一；支持页的分配、释放、读写；支持将数据表映射到页集合，模拟数据库物理存储结构 | read_page(page_id)、write_page(page_id, data) |
| 缓存管理 | 实现页缓存机制提升访问效率；支持 LRU（最近最少使用）与 FIFO（先进先出）替换策略；设计缓存命中统计与替换日志输出 | get_page(page_id)、flush_page(page_id)；命中率与替换日志 |
| 接口与集成 | 提供统一存储访问接口供数据库模块调用；与 SQL 编译器生成的执行计划对接，实现数据的物理访问 | 页级读写、缓存命中统计、页替换日志 |

### 模块三：小型数据库系统

| 组成部分 | 要求 |
|---|---|
| 执行引擎 | 解析并执行编译器生成的逻辑执行计划；至少实现 CreateTable、Insert、SeqScan、Filter、Project 基本算子；SeqScan 迭代访问表的所有数据页，Filter 按 WHERE 过滤，Project 按 SELECT 列表投影 |
| 存储引擎 | 调用底层页式存储接口（get_page、write_page 等）实现表数据在磁盘上的组织、存储与访问；实现记录（Row）与页（Page）的序列化 / 反序列化；管理空闲页列表，支持表扩展（分配新页）与回收 |
| 系统目录 | 维护元数据（表名、列名、列类型等）；系统目录本身作为一张特殊表（如 pg_catalog / sqlite_master）存储和管理；实现目录初始化、按表名查询表结构、建表后注册更新 |
| 接口与交互 | 提供命令行界面（CLI）或 API，输入 SQL 返回执行结果或错误信息；数据持久化：所有表数据与元数据通过页式存储系统落盘，程序重启后数据不丢失 |
| 核心操作 | CREATE TABLE（创建表结构并注册元数据）、INSERT（记录转二进制写入数据页）、SELECT 含 WHERE（SeqScan + Filter + Project 返回结果）、DELETE（定位记录并删除标记或物理删除） |

### 贯穿性验收标准（5 项）

| 标准 | 含义 |
|---|---|
| 正确 | 合法 SQL 能通过完整流水线，并得到结构正确的 AST 与 Plan |
| 稳健 | 非法输入不崩溃，能区分词法 / 语法 / 语义错误并准确定位 |
| 可扩展 | 文法、AST、Catalog、Plan 节点具有清晰模块边界 |
| 可优化 | 至少实现若干规则式优化，并能展示优化前后的差异 |
| 可验证 | 有系统测试、边界测试和教师隐藏测试结果 |

### 测试要求

1. **正常测试**：核心 SQL 正常执行；**词法错误**：非法字符、字符串未闭合；**语法错误**：缺分号、括号不匹配、结构错误；**语义错误**：表 / 列不存在、类型不匹配；**边界测试**：空输入、极长标识符、多语句、大小写。
2. **量化标准**：学生自建测试集 + 教师隐藏测试集，以通过率作为重要验收依据（隐藏测试是大模型时代最有效的正确性约束）。
3. **Fuzz 测试（扩展）**：随机生成 / 变异 SQL，检查指标为 Crash、Wrong Accept、Wrong Reject、Error Location；合法 SQL 应成功且结构稳定，非法 SQL 应拒绝且不崩溃。

### AI 使用边界与现场验收方式

大模型可以帮学生写代码，但不能替学生理解、测试与集成系统。

| 类别 | 内容 |
|---|---|
| 可使用的辅助方式 | 生成代码骨架、补全单元测试、辅助 Debug、比较不同 Parser 方案、解释编译错误、重构模块接口 |
| 必须自己承担 | 设计文法、定义 AST、明确语义规则、解释 Plan、验证优化等价性、对最终系统负责 |
| 不能通过验收的情况 | 代码能跑但讲不清、改一个需求就失效、隐藏测试大量失败、不知道错误属于哪个阶段、无法解释 AI 生成模块 |
| 现场验收 4 项 | ① 随机讲解：教师随机点 parseExpression / resolveColumn / buildPlan 等函数现场解释；② 需求修改：现场增加 <=、NOT、DISTINCT 等小需求并完成集成；③ 现场 Debug：给出失败 SQL 定位属于 Lexer / Parser / Semantic / Plan 哪一阶段；④ AST / Plan 推导：给一条新 SQL 现场画 AST 或逻辑执行计划 |

## 任务分级：Core / Advanced / Extension

核心范围稳定，扩展功能用于体现区分度，避免陷入单纯代码量竞赛。按以下优先级推进：

| 级别 | 内容 |
|---|---|
| 必做 Core | Lexer + Token 位置；四类 SQL（CREATE / INSERT / SELECT / DELETE）；AST；Catalog；语义检查；Logical Plan；错误定位；基础测试 |
| 进阶 Advanced | AND / OR / NOT 与复杂表达式；2 条以上优化规则；更强错误诊断；隐藏测试高通过率；Plan 可视化 |
| 扩展 Extension | JOIN / GROUP BY；Fuzz Testing；规则优化框架；简单代价模型；EXPLAIN；高级错误恢复 |

## 技术方案

### 开发语言与环境

建议使用 **Python 3.10+**（课程示例代码为 Python，开发调试成本低）；若小组有 Java / C++ 基础也可自选，但需保证命令行可运行、可自动化测试。所有代码放入统一项目目录 **database_system/**，建议使用 Git 管理版本。

### 项目目录结构（课程建议）

**database_system/ 目录结构**
```text
database_system/
|-- sql_compiler/          # SQL 编译器模块
|   |-- lexer.py           # 词法分析器
|   |-- parser.py          # 语法分析器
|   |-- semantic.py        # 语义分析器
|   |-- planner.py         # 执行计划生成器
|   `-- catalog.py         # 模式目录管理
|-- storage/               # 存储系统模块
|   |-- page.py            # 页式存储实现
|   |-- buffer.py          # 缓存管理实现
|   `-- file_manager.py    # 文件管理
|-- engine/                # 数据库引擎模块
|   |-- executor.py        # 执行引擎
|   |-- storage_engine.py  # 存储引擎
|   `-- catalog_manager.py # 系统目录管理
|-- cli/
|   `-- main.py            # 主程序入口（CLI）
|-- tests/
|   |-- test_sql.py        # SQL 编译器测试
|   |-- test_storage.py    # 存储系统测试
|   `-- test_db.py         # 数据库系统测试
`-- utils/
    |-- constants.py       # 常量定义
    `-- helpers.py         # 辅助函数
```

### 核心数据结构设计

| 结构 | 设计要点 |
|---|---|
| Token | 包含 type（KEYWORD / IDENTIFIER / CONST / OPERATOR / DELIMITER）、lexeme（词素值）、line、column；位置信息决定错误诊断能否精确到行列 |
| AST 节点 | 语句节点：CreateTableStmt、InsertStmt、SelectStmt、DeleteStmt；表达式节点：BinaryExpr、UnaryExpr、IdentifierExpr、LiteralExpr；携带表名 / 列名、操作符、子表达式、源码位置，类型由语义阶段补充 |
| Catalog | 等价于编译原理中的符号表（Symbol Table）；维护表 → 列 → 类型 的层级结构；接口 createTable / findTable / findColumn / getType |
| Plan 节点 | CreateTable、Insert、SeqScan、Filter、Project；只保存执行所需信息，不携带无关语法细节；输出形式树形 / JSON / S 表达式任选 |

### MiniSQL 支持范围

必做语言能力示例（四类语句 + WHERE 条件 + 比较运算 + AND / OR / NOT + 括号）：

**核心 SQL 子集示例**
```sql
CREATE TABLE student( id INT, name VARCHAR, age INT );
INSERT INTO student(id,name,age) VALUES (1,'Alice',20);
SELECT id, name FROM student WHERE age > 18 AND id != 3;
DELETE FROM student WHERE id = 1;
```

扩展能力（可选）：UPDATE、ORDER BY、GROUP BY、JOIN、算术表达式、NULL、更多数据类型。原则：不追求 SQL 越多越好，重点提升编译器结构与正确性。

### 规则式优化（至少实现 2 条以上）

1. **常量折叠**：age > 10 + 8 → age > 18
2. **布尔化简**：x AND TRUE → x
3. **投影裁剪（Projection Pruning）**：只保留查询真正需要的列
4. **谓词下推（Predicate Pushdown）**：有 JOIN / 子查询扩展时尽量提前过滤
5. **冗余节点消除**：Project[*] / 恒真 Filter 可直接删除

验收重点是：规则正确、前后结构可验证、结果语义等价，而不是优化多复杂。

## 工作分解与实施步骤（WBS）

共 8 个阶段，前 5 个阶段完成 SQL 编译器，随后进入存储与引擎，最后集成测试。每个阶段给出输入、输出与完成定义（Definition of Done）。

### 阶段 0：规则定义（准备）

**任务**：明确 SQL 子集的词法规则、语法规则与语义规则；建立关键字表、标识符表、常量表示方式；初始化 Catalog 结构；编写并提交 grammar.md（文法必须与代码实现一致）。**输出**：grammar.md + 常量定义 + 项目骨架。**完成定义**：文法文档评审可读，四类语句产生式完整，表达式优先级（NOT > 比较 > AND > OR）表达正确。

### 阶段 1：词法分析器

**任务**：从源程序逐字符读入，切分单词；对正确单词输出 [种别码, 词素值, 行号, 列号]；处理注释、字符串转义、多字符运算符（>=、<=、!=）、大小写；对非法单词输出错误类型 + 位置。**输出**：lexer.py + Token 结构 + 词法测试。**完成定义**：示例 SQL 输出正确 Token 流；@、未闭合字符串、非法数字均报错且不崩溃。

### 阶段 2：语法分析器 + AST

**任务**：基于 grammar.md 实现 Parser（推荐递归下降，结构清晰易构造 AST）；构造语句节点与表达式节点；实现语法错误诊断（位置 + unexpected token + expected 集合）。**输出**：parser.py + AST 定义 + 语法测试。**完成定义**：四类语句全部解析成功；缺分号、括号不匹配等错误可定位；a = 1 OR b = 2 AND c = 3 的 AST 体现 AND 优先级高于 OR。

### 阶段 3：语义分析 + Catalog

**任务**：实现表 / 列存在性检查、名字绑定、类型一致性检查、INSERT 匹配检查；实现 Catalog 的创建与查询；集中管理类型规则。**输出**：semantic.py + catalog.py + 语义测试。**完成定义**：SELECT score FROM student（score 不存在）报 SemanticError；INSERT 类型 / 列数不匹配报 TypeMismatch，均带位置与原因。

### 阶段 4：执行计划生成 + 优化

**任务**：AST → Logical Plan 转换（FROM → SeqScan，WHERE → Filter，SELECT 列表 → Project）；实现至少 2 条规则式优化；提供 Plan 输出（树形 / JSON / S 表达式）。**输出**：planner.py + 优化器 + Plan 输出测试。**完成定义**：SELECT name FROM student WHERE 1 = 1 AND age > 10 + 8 优化前后结构可对比（Filter 变为 age > 18）。

### 阶段 5：页式存储系统 + 缓存

**任务**：实现页结构（固定大小、页号唯一）、页分配 / 释放 / 读写；实现缓存（LRU / FIFO 可切换）、命中统计与替换日志；提供 read_page / write_page / get_page / flush_page 接口。**输出**：storage/ 模块 + 存储测试。**完成定义**：页读写正确，LRU / FIFO 命中率可统计，替换日志可输出。

### 阶段 6：执行引擎 + 存储引擎 + 系统集成

**任务**：实现 CreateTable、Insert、SeqScan、Filter、Project 算子；实现 Row ↔ Page 序列化 / 反序列化、空闲页管理；将系统目录作为特殊表持久化；实现 CLI 主程序；接通编译器 → 执行计划 → 存储全链路。**输出**：engine/ + cli/main.py + 集成测试。**完成定义**：CREATE / INSERT / SELECT（含 WHERE）/ DELETE 全部可执行；重启程序后数据仍在。

### 阶段 7：测试完善、报告与演示

**任务**：扩充自建测试集（正常 + 词法错误 + 语法错误 + 语义错误 + 边界）；可选 Fuzz 测试；整理运行结果与失败案例分析；编写实践报告；准备端到端演示 SQL 与答辩讲解。**输出**：tests/ 全套 + 测试报告 + 实践报告 + 演示脚本。**完成定义**：所有 Core 验收项通过，隐藏测试预期高通过率，能现场讲解与演示。

## 里程碑与排期（建议）

以下排期按每天投入 2～3 小时、约 3 周完成估算，起止日期以课程通知为准，可整体平移。里程碑退出条件是进入下一阶段的硬门槛。

| 里程碑 | 建议时间 | 交付物 | 退出条件 |
|---|---|---|---|
| M1 规则 + 词法 | 第 1-3 天 | grammar.md、lexer.py、Token 流输出 | 示例 SQL 输出正确 Token 流；非法字符报错带位置 |
| M2 语法 + AST | 第 4-6 天 | parser.py、AST 定义、语法测试 | 四类语句解析成功；语法错误带位置 + 期望符号 |
| M3 语义 + Catalog | 第 7-8 天 | semantic.py、catalog.py、语义测试 | 表 / 列不存在、类型不匹配均报错且定位准确 |
| M4 Plan + 优化 | 第 9-10 天 | planner.py、优化器、Plan 输出 | SELECT 输出 Project / Filter / SeqScan；2 条优化规则生效且可对比 |
| M5 页式存储 + 缓存 | 第 11-13 天 | storage/ 模块、缓存统计与日志 | 页读写正确；LRU / FIFO 命中率可统计 |
| M6 引擎 + 集成 | 第 14-16 天 | engine/、cli/main.py、全链路演示 | 四类 SQL 全链路执行成功；重启数据不丢失 |
| M7 测试完善 | 第 17-18 天 | 全套测试用例 + 运行结果 | 自建测试全覆盖，隐藏测试预期高通过率 |
| M8 报告 + 答辩 | 第 19-21 天 | 实践报告、演示 SQL、答辩材料 | 能现场讲解、现场改需求、现场 Debug |

## 团队分工建议

指导书要求"独立完成"，若课程允许组队（2～3 人），建议按模块边界分工，接口先行、各自独立测试：

| 角色 | 负责模块 |
|---|---|
| 成员 A（前端） | 词法 + 语法：lexer.py、parser.py、grammar.md、AST 定义、语法错误诊断 |
| 成员 B（中间层） | 语义 + 计划：semantic.py、catalog.py、planner.py、优化器、Plan 输出 |
| 成员 C（后端） | 存储 + 引擎 + 集成：storage/、engine/、cli/main.py、持久化、系统目录表 |
| 全员 | 测试集共建、Fuzz（可选）、实践报告、演示与答辩准备 |

## 风险与应对

| 风险 | 预警信号 | 应对动作 |
|---|---|---|
| 表达式优先级 / 左递归处理错误 | 复合条件解析结果与预期不符 | 先写 AST 打印 / 单元测试逐层验证；用 a = 1 OR b = 2 AND c = 3 等样例回归；理解 NOT > 比较 > AND > OR |
| 隐藏测试通过率低 | 自建测试只覆盖正常路径 | 尽早自建边界测试（空输入、超长标识符、多语句、大小写）与错误注入；可选 Fuzz 找崩溃点 |
| 现场讲不清 AI 生成的代码 | 被点 parseExpression / buildPlan 无法解释 | 每个模块亲手重写核心逻辑、加注释；整理"设计决策文档"；提前模拟随机讲解与现场改需求（<=、NOT、DISTINCT） |
| 持久化数据丢失 | 重启后表或数据消失 | 先内存实现打通流程，再接入页式落盘；用"重启后查询"作为验收用例 |
| 时间不足 | 进度落后于里程碑 | 严格执行 Core 优先、Advanced 次之、Extension 最后；收缩扩展范围，保证主线闭环（SQL → Plan → 存储 → 执行） |

## 最终交付物与验收清单

交付物从"程序"升级为"可验证的软件模块"，共 6 类：

| 类别 | 内容 |
|---|---|
| 源码 | 模块化源码 + README + 构建 / 运行说明 |
| 设计 | grammar.md + AST / Catalog / Plan 结构说明 |
| 测试 | 测试用例 + 运行结果 + 失败案例分析 |
| 输出 | Token → AST → Semantic → Plan → Optimized Plan 全链路输出示例 |
| 报告 | 关键设计决策、问题定位过程、AI 辅助使用说明 |
| 演示 | 附一条端到端演示 SQL |

### 实践报告格式（按指导书附件）

1. 实践目的：此次实践应达到的学习目标
2. 实践内容：此次实践应完成的任务
3. 实践方法：实践方法、原理、技术、方案等
4. 实践步骤：完成该实践的操作步骤
5. 实践结果：记录实践输出数据和结果
6. 实践结论：对数据与结果分析，给出成果和结论
7. 实践小结：体会、遇到的问题与解决办法、待改进之处

注：有程序须附源代码，有图表须有截图并有文字说明和分析。报告应包含词法、语法、语义分析的详细设计思路，以及存储管理、缓存机制、访问接口设计与测试方法的完整说明。

## 执行节奏建议

1. **第 1 周（编译器前端）**：完成阶段 0-3，实现词法 → 语法 → 语义，提交 grammar.md 与三个模块的测试，达到 M1-M3 退出条件。
2. **第 2 周（计划与存储）**：完成阶段 4-5，实现执行计划、优化与页式存储 + 缓存，达到 M4-M5 退出条件。
3. **第 3 周（集成与验收）**：完成阶段 6-7，打通全链路、补测试、写报告、练演示，达到 M6-M8 退出条件。

**每日自检三问**：今天产出是否可运行？是否写了对应测试？关键函数能否现场讲清？——这是大模型时代通过验收的核心。
